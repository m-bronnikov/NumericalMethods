#include "Matrix/Matrix.h"

int main(){
    /*
    QMatrix matrix;
    RVector a;
    RVector b;
    read_matrix(&matrix, "data/m1.txt");
    print_matrix(&matrix);

    lancosh_method(&matrix, &a, &b, 0);

    delete_RVector(&a);
    delete_RVector(&b);
    delete_matrix(&matrix);
    */
   free(NULL);
    return 0;
}